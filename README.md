# chatgp
to learn
# Review Request MVP

Minimal FastAPI service for managing businesses, contacts, campaigns, and queued review-request sends. Includes a basic front-end UI.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
